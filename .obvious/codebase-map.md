# Codebase Map

| Directory | Purpose |
|---|---|
| `images/` | Art piece images — full-size JPGs, thumbnails, nav thumbnails, and PNG icons |
| `stylesheets/` | Single CSS file (`styles.css`) — layout, map popups, thumbnail nav, marker clusters |
