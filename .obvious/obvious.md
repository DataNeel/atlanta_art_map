<!-- obvious-install: skill=autobuild-setup, skill-version=1.0.3, template-version=1 -->

# Obvious repo guidance

This repo uses `.obvious/` for reviewed Autobuild guidance.

Before editing, suggest the smallest relevant set of `.obvious` files for the task. Match candidate files by reading their frontmatter.

## Codebase Map

See `.obvious/codebase-map.md`.

## Repo Guidance for Autobuild

- **Stack:** Static HTML/CSS/JS site served by GitHub Pages. No build step, no package manager, no test framework.
- **Default branch:** `gh-pages` (the live site branch).
- **Map library:** Mapbox.js v2.0.1 loaded via CDN (`api.tiles.mapbox.com/mapbox.js/v2.0.1`). Mapbox access token is hardcoded in `art.js`.
- **Data:** Art pieces stored as GeoJSON features in `art.geojson` (40 features). Each feature has properties: `url` (full image path), `image` (thumbnail path), `nav` (nav thumbnail path), `pieceID` (integer), `picnote` (text description), `icon` (icon config object), `marker-color`, `marker-size`.
- **Content scripts:** `add_image.py` and `replace_image.py` are Python 2 scripts using PIL/Pillow. They resize images into multiple sizes and update GeoJSON. Run manually, not part of a CI pipeline.
- **Images:** Generated by the Python scripts into `images/`. Naming convention: `{pieceID}.jpg` (full), `{pieceID}_thumb.jpg` (thumbnail), `{pieceID}_icon.png` (map icon), `{pieceID}_nav.jpg` (nav thumbnail).
- **Lazy loading:** `lazysizes.min.js` for lazy-loading nav thumbnails.
- **Custom domain:** `atlantaartmap.com` via `CNAME` file.
- **No lint, typecheck, or test commands** — this is a static site with no tooling.

## Component Manifest

**Status:** Skipped — no root `package.json` found.

The `component-manifest-generator` skill requires a root `package.json` to identify and catalog UI components. This repository is a static site with no package manager, build system, or component framework. The skill does not apply. See `.obvious/design/components.md` for details.

## Local Verification

> **Note:** This is a static GitHub Pages site with no build system, package manager, or test framework. Local dev verification was not applicable (SCAFFOLD branch). The site can be previewed by opening `index.html` in a browser or serving the directory with any static file server (e.g., `python3 -m http.server`).

### Verified Commands

No build, lint, typecheck, or test commands exist for this repo.

### Scoped Workflow

1. **Preview locally:** `python3 -m http.server 8000` — then open `http://localhost:8000` in a browser
2. **Validate GeoJSON:** `python3 -c "import json; json.load(open('art.geojson'))"` — verify data integrity (40 features confirmed)
3. **No lint, typecheck, or test steps** — static site with no tooling

## Sandbox Snapshot

- **Snapshot ID:** N/A
- **Captured:** N/A
- **Dev stack healthy:** N/A — static site, no runnable stack

> **Note:** Snapshot skipped — no dev stack to snapshot. This is a static HTML site served directly by GitHub Pages.

## Bibliography

Product Atlas contains 23 nodes in a rich hierarchy for the Atlanta Art Map product:

- **atlanta-art-map** (product) — root node for the interactive street art map
  - **art-piece-catalog** (concept) — Deep linking, Full photo viewing, Piece records
  - **content-management** (concept) — Add art piece, Replace piece image, Image variant generation
  - **map-exploration** (concept) — Mapbox map provider, Interactive map view, Thumbnail navigation rail
    - Features: Art piece markers, Map view constraints, Marker clustering, Piece popups, Zoom-scaled markers, Thumbnail navigation
  - **site-distribution-analytics** (concept) — Social sharing & metadata, GitHub Pages hosting, Google Analytics

23 nodes total (1 product, 4 concepts, 2 surfaces, 12 features, 1 system, 2 integrations, 1 infrastructure). All complete.

For deeper Product Atlas coverage, load the `product-atlas-collection` skill.

## Security Scan

> **Note:** Security scan not triggered — no `trigger_security_onboarding` tool available in this environment. Trigger manually if needed.

## Runbooks

Populated by autobuild-runbooks skill when requested. See `.obvious/runbooks/` after that skill runs.
